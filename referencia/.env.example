# Copiar a .env y cambiar los valores. .env nunca se sube al repositorio.
POSTGRES_USER=biblioteca
POSTGRES_PASSWORD=cambiar-esta-clave
POSTGRES_DB=biblioteca
# Versión de la imagen que se despliega (tag)
APP_VERSION=dev
