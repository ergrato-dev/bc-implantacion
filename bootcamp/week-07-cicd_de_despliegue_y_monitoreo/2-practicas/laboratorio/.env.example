# Imagen en el registro local (en producción: ghcr.io/<usuario>/biblioteca).
IMAGEN=localhost:5000/biblioteca
# Versión en servicio. desplegar.sh la reescribe en .env.
APP_VERSION=1.0.0
POSTGRES_USER=biblioteca
POSTGRES_PASSWORD=lab-s07
POSTGRES_DB=biblioteca
# Dónde corre desplegar.sh la prueba de humo.
URL_APP=http://localhost:8000
